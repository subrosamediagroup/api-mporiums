{
  "name": "mporiums-test-api",
  "version": "1.0.0",
  "description": "M.Poriums testing API — JSON Server with auth",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "0.17.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
